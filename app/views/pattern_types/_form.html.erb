<%= form_for(@pattern_type) do |f| %>
  <% if @pattern_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pattern_type.errors.count, "error") %> prohibited this pattern_type from being saved:</h2>

      <ul>
      <% @pattern_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :pattern_text %><br>
    <%= f.text_area :pattern_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
